<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Chatly" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="fontFamily">@font/roboto_regular</item>
    </style>

    <style name="Theme.Chatly" parent="Base.Theme.Chatly" />

    <style name="Theme.Chatly.Splash" parent="Theme.SplashScreen">

        <item name="windowSplashScreenBackground">@color/bw_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Chatly</item>


    </style>
</resources>